# Memory Intelligence Service - Environment Variables
# Copy this file to .env and update with your values

# Python Version (for Render)
PYTHON_VERSION=3.11.0

# Server Port (Render sets this automatically)
PORT=10000

# Database Configuration (Optional - leave empty if not using database features)
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=3306

# Service Configuration
SERVICE_ENV=production
DEBUG=false
